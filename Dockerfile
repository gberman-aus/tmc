# Will update.

# For now, here's a list of things we need to do in the Docker file:

# 1. Download Ubuntu 22.04
# 2. Install R
# 3. Install Python
# 4. Install git
# 5. Set the $HOME library for R packages, as per https://docs.rc.fas.harvard.edu/kb/r-packages-with-singularity/
# 6. Install the relevant R packages (stm, yaml, igraph) and their dependencies
# 7. Install the relevant Python librarys (PyYaml, beautifulsoup4, numpy, pandas) and their dependencies

# 8. From git download the 'production' version of TopicModelContainers 
